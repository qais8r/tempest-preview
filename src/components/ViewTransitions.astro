---
import { transitionScript } from '../lib/transition-script';

interface Props {
  root: string;
}
const { root } = Astro.props;
const script = await transitionScript;
---

<style is:inline>
  @view-transition {
    navigation: auto;
  }
  @media (prefers-reduced-motion: reduce) {
    @view-transition {
      navigation: none;
    }
  }
</style>
<!-- This must run in the head before the first render, including on a cold navigation. -->
<script is:inline data-transition-root={root} set:html={script} />
